function BillingHistoryPage() {
  return (
    <section className="dashboard-card p-6">
      <h1 className="text-2xl font-display text-text">Billing History</h1>
      <p className="mt-2 text-text-muted">
        Placeholder page in the shared dashboard shell. Add invoices, filtering, and export actions when backend endpoints are ready.
      </p>
    </section>
  );
}

export default BillingHistoryPage;
